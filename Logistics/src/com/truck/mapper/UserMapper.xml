<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.truck.dao.IUserDao">
	
	<select id="selectUserById" parameterType="int" resultType="UserBean">
		select u_id as id,u_name as name,u_pass as pass,u_realname as realName,u_idnumber as idNumber,u_sex as sex,u_role as role from t_user where u_id=#{id};
	</select>

	<select id="selectUserByName" parameterType="String" resultType="UserBean">
		select u_id as id,u_name as name,u_pass as pass,u_realname as realName,u_idnumber as idNumber,u_sex as sex,u_role as role from t_user where u_name=#{name};
	</select>
</mapper>